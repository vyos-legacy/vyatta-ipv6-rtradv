type: u32

help: Set maximum interval between unsolicited multicast RAs

# Default value per RFC-4861
default: 600

comp_help: Possible completions:
 <4 - 1800>\tMaximum interval in seconds between unsolicited multicast RAs

syntax:expression: ($VAR(@) >= 4 && $VAR(@) <= 1800) ; "Value must be between 4 and 1800"
