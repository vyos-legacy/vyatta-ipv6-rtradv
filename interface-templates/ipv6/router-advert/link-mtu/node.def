type: u32

default: 0

help: Set value of link MTU to place in RAs

comp_help: Possible completions:
 <1 - MAX>\tValue of link MTU to place in RAs
 0\t\tDo not send MTU options in RAs

syntax:expression: ($VAR(@) == 0 || $VAR(@) >= 1280) ; "Value must be 0 or 1280 or greater"
